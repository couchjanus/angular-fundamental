// array.type.ts

// Массивы могут описываться двумя способами. 
// В первом нужно указывать тип элементов перед '[]'

var list: number[] = [1, 2, 3];


let list1: number[] = [10, 20, 30];
let colors: string[] = ["red", "green", "blue"];
console.log(list1[0]);
console.log(colors[1]);

let numderArray: String[] = ['a', 'b', 'c']; // Defines a string array variable

let randomArray: any[] = ['a', 1, true];

